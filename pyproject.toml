[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "options-analysis-engine"
version = "2.0.0"
description = "European option pricing, derivative verification, and reproducible market diagnostics"
requires-python = ">=3.10"
license = {file = "LICENSE.txt"}
dependencies = ["numpy>=1.24,<3", "scipy>=1.10,<2", "pandas>=2.0,<3", "matplotlib>=3.7,<4", "tzdata>=2023.3"]

[project.optional-dependencies]
app = ["streamlit>=1.32,<2", "yfinance>=0.2.54,<2"]
dev = ["pytest>=8,<10"]

[project.scripts]
options-engine = "options_engine.cli:main"

[tool.setuptools.packages.find]
include = ["options_engine*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
